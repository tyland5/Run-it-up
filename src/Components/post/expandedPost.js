import {StyleSheet, Text, View, Button } from 'react-native';

export default function ExpandedPost(){
    return(
        <View>
            <Text style={{color: "white", fontSize: 28}}>TESTING </Text>
        </View>)
}